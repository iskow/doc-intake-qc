# STATUS.md — Document Intake QC Agent

**Repo:** https://github.com/iskow/doc-intake-qc.git (push here at each phase close)
**Active:** yes (priority 1 of 5 — see PROJECT-STANDARD.md build order)
**Current phase:** Phase 5 — **5a complete, committed and pushed** (`55a37c9`, `18281a1..55a37c9`). Gate passed (PASS 85 / FAIL 0), proven with three negative tests, re-run green on the working tree immediately before the push; all five prior gates re-run green. Phase 5 was split at kickoff: **5b is the portfolio close** — case-study skill, qc-gate skill, SOP-DRAFT final. Awaiting Joel's approval before 5b starts.
**Last updated:** 2026-07-21 (Phase 5a: run_demo.py, README checked against the code, screenshot staleness guard)

## Done
- Plan approved by Joel (his go on Phase 0 doubled as plan QC).
- `scripts/make_mock_data.py` — generates the fixture AND the answer key from one data structure (they can't drift). Rerunnable: delete `mock-intake/` and rerun.
- `mock-intake/` — 41 files (+2 inside the zip), 6 folders incl. 4-level nesting.
- `seeded-errors.md` / `.json` — 21 seeded errors across 9 types (exact-duplicate ×3 pairs, near-duplicate-name, naming-violation ×6, extension-mismatch ×3, zero-byte ×2, date-anomaly ×2, junk-file ×2, archive, deep-nesting).
- `scripts/qc_phase0.py` — the Phase 0 QC gate as a rerunnable script.
- **Phase 1: `scripts/scan.py`** — read-only walk of `mock-intake/` → `manifest.csv` (41 rows). Columns: path, name, extension, true_type (magic-byte detection), size_bytes, sha256, created, modified. Archives listed as single files (not expanded). Reuses the chunked-SHA-256 pattern and grabs the type-sniff head in the same pass.
- **Phase 1: `scripts/qc_phase1.py`** — gate script: runs scan, then checks row count vs disk vs answer key, path-set absence check, dupe-pair hash match, no blank cells, archive-not-expanded, and a before/after read-only fingerprint.
- Git: repo initialized, `.gitattributes` (`* -text` — protects fixture hashes from CRLF conversion), commit `7f7fd7e` pushed to origin/main. `manifest.csv` added to `.gitignore` (generated artifact, hashes rotate per run).
- **Phase 2: `scripts/rules.py`** — reads `manifest.csv` with pandas, applies **nine** rules, writes `exceptions.csv` (path, name, rule_id, severity, detail). One row per (file, rule). Result on the fixture: **30 exceptions across 23 files; 18 files clean; 41 total.** Rules: EXACT_DUP, EXT_MISMATCH, ZERO_BYTE (high); NEAR_DUP_NAME, DATE_ANOMALY, JUNK_FILE, ARCHIVE (medium); NAMING, DEEP_NESTING (low). Nine not six — see DECISIONS 2026-07-21.
- **Phase 2: `scripts/qc_phase2.py`** — gate script: runs scan → rules, then absence-checks every seeded error against its owning rule, asserts **zero false positives** on the 18 clean files, reconciles totals, verifies the author column against the answer key, and checks every exception row is well-formed. Read-only proven by before/after fingerprint.
- **Phase 2: author column in `scan.py`** — `read_author()` reads PDF `/Author` (pypdf), DOCX/XLSX creator (python-docx/openpyxl); `Unassigned` where the format can't carry one or the file won't parse as its claimed type. Verified **0 mismatches** vs the answer key's `authors` map (41/41). pypdf's warning logger silenced (expected noise on the text-as-PDF file).
- **Phase 2 gate proven with a negative test:** disabling ZERO_BYTE made the gate fail with 4 failures naming both empty files (exit 1); reverted and re-passed. The gate has teeth.
- Phase 1 gate re-run after the `scan.py` change: still PASS 13 / FAIL 0 — no regression.
- **2026-07-20 — Document-author enrichment (Phase 0 amendment):** `make_mock_data.py` now embeds a document author (PDF `/Author`, DOCX/XLSX creator) — 22 files authored, 19 Unassigned. Answer key gained an `authors` map. One divergence file (`CTR-2026-01_..._AcmeSupply.docx`) authored by Meridian to prove author ≠ party ≠ custodian. Fixed reportlab's `"anonymous"` default. Verified: embedded authors match the answer key (0 mismatches); `qc_phase0.py` still PASS 39/0. See DECISIONS 2026-07-20. Not yet committed/pushed.

- **2026-07-21 — Phases 1–2 pushed.** Both gates re-run green on the working tree first (Phase 2: PASS 22 / FAIL 0; Phase 1: PASS 13 / FAIL 0, no regression), then two phase-sized commits: `d7de3e5` (Phase 0 author amendment + Phase 1 scanner) and `0b152bc` (Phase 2 rules engine + docs). Pushed `6ef6525..0b152bc`; working tree clean. Stray 0-byte `.__wtest` (write-probe orphan, unreferenced by any script) removed, not committed.
- **2026-07-21 — Phase 3 LLM decided:** local open-source instruct model via **Ollama**, not a hosted API. Full reasoning and the rejected options (DeepSeek V4, hosted free tiers) in DECISIONS 2026-07-21.

- **2026-07-21 — Phase 3 complete (this session).** Built in the order STATUS demanded: measure first, build second.
  - **Load-bearing step done first.** Two throwaway spikes before any pipeline code. Spike 1: `gemma4:12b` scored **12/12** on text alone, both traps caught, ~1.7s/doc. Spike 2 probed the ambiguous and no-text files and produced the session's two key findings — (a) confidence is pinned at 0.9–1.0 on everything, so it cannot gate anything, and (b) the draft text extractor was scraping binary garbage out of PNGs (541 chars from a solid-color image) and feeding it to the model.
  - **Phase 0 amendment — class ground truth.** `make_mock_data.py` gained `DOC_CLASSES` (41 files) and writes a `classes` map into `seeded-errors.json`; ambiguous files carry a list of acceptable labels, no-text files carry `null`. Two set-difference checks fail the generator loudly if the table and fixture drift. Fixture regenerated; Phase 0/1/2 gates re-run green first (39/0, 13/0, 22/0).
  - **`scripts/classify.py`** — reads `manifest.csv`, extracts text (dispatching on `true_type`, **no** catch-all fallback), calls `gemma4:12b` via stdlib `urllib` at `127.0.0.1:11434` (temperature 0, `format=json`), writes `classifications.csv` (path, name, chars, label, confidence, folder_hint, status, reason). **The prompt contains document text only — never the filename or folder.** 41 files in ~43s.
  - **`scripts/rules.py` — three new rules** (`UNCLASSIFIED`, `CLASS_CONFLICT`, `LOW_CONFIDENCE`), running only when `classifications.csv` exists so Phase 2 still stands alone without Ollama. Result: **39 exceptions across 25 files** (30 deterministic + 9 UNCLASSIFIED).
  - **`scripts/qc_phase2.py`** scoped to its own nine rule IDs — otherwise a site photo's `UNCLASSIFIED` row reads as a false positive. Still 22/0.
  - **`scripts/qc_phase3.py`** — 17 checks: coverage, accuracy vs the answer key, no-guessing, no-silent-drops, an independently recomputed exceptions absence check, well-formedness, determinism sampling, loopback-endpoint (=$0), and a read-only fingerprint.
  - `classifications.csv` added to `.gitignore` (generated artifact, same reasoning as `manifest.csv`).

- **2026-07-21 — Phase 4 part 1 complete (this session): the non-destructive organizer.** Phase 4 was split at kickoff — organize now, the client-facing HTML report next session — so the deliverable that gets screenshotted for the case study gets a full sitting.
  - **`scripts/organize.py`** — `--by class|author|custodian`, copying all 41 files into `organized/by-<mode>/<value>/` under their **original names**. Three axes from three separate sources: the Phase 3 label, the Phase 2 embedded-author column, and a new declared mapping table. `--dry-run` reports buckets without writing. `shutil.copy2` (not `copy`) preserves mtimes so the seeded date anomalies survive.
  - **`custodian-map.csv`** (committed, new) — ordered `pattern,custodian,source_note` rules, first match wins, standing in for a client's collection log. Two path-specific rows sit above the folder rules to prove precedence. Unmatched → `Unassigned`.
  - **Bucket results:** class = 6 buckets, 9 Unassigned. author = 6 buckets, 19 Unassigned. custodian = 8 buckets, 9 Unassigned. Every mode totals 41. The class `Unassigned` bucket holds exactly the 9 files predicted last session (5 photos, the archive, `Thumbs.db`, 2 empty files) — verified on disk with PowerShell, independently of the Python that created it.
  - **The divergence file is now proven in code.** `CTR-2026-01_ServiceAgreement_AcmeSupply.docx` lands in `contract/` by class, `Dana Cruz/` by author, and `K. Aguilar (Legal)/` by custodian — three axes, three different answers, one document. The gate asserts all three differ, so the case study's central claim cannot quietly stop being true.
  - **`scripts/qc_phase4.py`** — 36 checks: read-only fingerprint, completeness, per-file hash equality, mtime preservation, bucket correctness recomputed from the answer key, exact `Unassigned` membership, named custodian precedence cases, axis independence, the `--out` safety guard, and idempotence.
  - **Two PLAN amendments, both logged in DECISIONS:** files are copied **as-is, not renamed** (Joel's call — traceability over tidiness; the naming gap is now an explicit reporting obligation), and output goes to `organized/by-<mode>/` rather than `organized/<value>/` so the three axes don't merge into one tree.
  - `organized/` added to `.gitignore`.

- **2026-07-21 — Phase 4b complete (this session): metadata fidelity.** Built measure-first again: three throwaway spikes before any pipeline code, and the spikes changed the design twice.
  - **Joel answered both open questions.** (1) Preserve everything — ACLs, owner, alternate data streams, not just timestamps. That disqualifies ctypes (timestamps only) and selects robocopy. (2) A filename collision must **fail loudly** with a warning, never auto-suffix `__2`.
  - **`scripts/organize.py` — engine swapped to batched `robocopy /COPY:DATSO /DCOPY:DAT`.** The standing objection to robocopy (a process per file) is answered by batching **one call per (source folder → destination folder)**: invocations scale with folder count, not file count. Batches capped at 8,000 chars against the Windows command-line limit. Exit codes checked (0–7 = success) because robocopy fails silently otherwise. `copy2` demoted to a loud non-Windows fallback.
  - **Nested layout.** Destination is now `by-<mode>/<bucket>/<original path under the intake>/<file>`. The 4-level `Old Files/2019 archive/deep/nested/` chain survives into every bucket.
  - **Collision preflight.** The whole plan is checked before a byte moves; a clash prints both source paths and exits non-zero having copied nothing. Note it can **never fire on real input** — preserving relative paths makes collisions structurally impossible — so the gate proves it fires on a synthetic plan *and* stays silent on the real one.
  - **`organized/crossref-by-<mode>.csv`** — mandatory from 4b. original_path, original_folder, bucket, new_path, sha256, size, created, modified. Written **beside** the tree, never inside it, so "every file in this tree is a copy of an original" stays true. The hash is the scan-time hash from `manifest.csv`, not recomputed — provenance from before any copy existed.
  - **`scripts/qc_phase4.py` — 36 → 73 checks.** Added: creation-time equality on all 41 copies × 3 modes; structure reconstructable by asking the *filesystem* (strip the bucket, find the file back in the intake); an anti-vacuity check that the tree is actually nested 4+ deep; full cross-reference reconciliation; a **copy-engine probe**; and the two-way collision test.
  - **The engine probe matters.** The fixture has no alternate data streams and no interesting ACLs, so checking those on it would pass vacuously. Instead the gate plants a temp file carrying an ADS, an explicit non-inherited ACE and three distinct timestamps, runs the **real production copy function** over it, and verifies all of it — the same technique the `--out` guard uses.
  - **Three negative tests, each isolating one claim.** (A) Forcing the copy2 fallback → 4 failures, all three modes' creation checks plus the probe; `modified` still passed, proving the creation check is what discriminates. (B) Flattening the layout → 15 failures including "nesting survived: 0 copies sit 4+ folders deep." (C) Dropping `S` and `O` from the robocopy flags → **exactly 1** failure, the ACL probe. All reverted; 73/0 restored.
  - **Test C exposed a vacuous check and it has been relabelled, not left to flatter us.** Dropping `/COPY:O` did *not* fail the owner check, because every file on this machine is owned by the same user. The check now reads "owner not clobbered" and the real limitation is logged as an open RISK.
  - **PLAN amended mid-phase, and a requirement was WITHDRAWN rather than quietly failed.** 4b was planned to require "every copy's **three** timestamps match its source." Access time cannot be preserved end-to-end: reading a file updates it (a Python hash read moved one from 2001 to now, and `scan.py` hashes all 41 files in Phase 1), and NTFS defers atime writes to disk. On the fixture 39 of 41 copies disagreed with their source's atime; the only 2 that matched were the **zero-byte** files, which are never read. Created and modified matched exactly throughout. Access fidelity is proven on the engine in isolation and stated as a limitation for 4c.
  - **A logged fact was found to be wrong and corrected:** the earlier entry claiming `copy2` carries neither ACLs, owner, nor alternate data streams is wrong about ADS — Python ≥3.8 routes `copyfile` through Win32 `CopyFile2`, which copies streams. Verified by stream enumeration. Corrected in place with the original left visible.
  - **No regression:** Phase 0/1/2/3 gates re-run green (39/0, 13/0, 22/0, 17/0). No new dependencies — `subprocess` and `os` from the standard library.

- **2026-07-21 — Phase 4c complete (this session): the client-facing HTML QC report.**
  - **`scripts/report.py`** — reads manifest / exceptions / classifications / the three cross-references and writes **one self-contained `qc-report.html`** (34 KB, inline CSS, inline SVG charts, **zero** external assets, zero JavaScript). Sections: summary → one queue → classification → three-axis comparison → date fidelity → cross-reference → reconciliation → limitations.
  - **It refuses to render numbers it cannot prove.** Eight reconciliation checks run before the file is written and a mismatch exits non-zero: files vs classification rows, flagged + clean vs received, severities vs total findings, unclassified label count vs UNCLASSIFIED findings, each mode's cross-reference vs received, and timestamp comparisons matching vs made. All 8 green on the fixture, and the table is rendered *into* the report so the client sees the check, not just the result.
  - **Date fidelity is re-measured at render time, not quoted from 4b.** 41 files × 3 modes × 2 fields = **246/246 comparisons match** their pre-copy manifest record. A lossy re-copy would show up the next time the report runs.
  - **`scripts/rules.py`** gained `RULE_DOCS`, `SEVERITY_DOCS`, `SEVERITY_ORDER` (additive only — no behavior change, Phase 2 gate still 22/0). The report imports them so client-facing wording and the rule that produced the finding live in one file.
  - **`scripts/qc_phase4.py` — 73 → 131 checks.** The report section recomputes **21 figures** from the CSVs and compares each against the value parsed back out of the **rendered HTML** (`data-qc` attributes) — it never trusts report.py's arithmetic. Plus: all 7 required sections present, every fired rule explained, the divergence file shown in 3 distinct buckets, the four honest-limitation statements still present, and 5 patterns asserting no external asset.
  - **Palette computed, not eyeballed — and the brand colors failed.** Run through the dataviz validator: Sage on Mist is **1.69:1** and the brand trio spans 81° of hue, so Deep Teal/Steel Teal/Sage is *not* a usable severity ramp. Replaced with a validated one-hue teal ordinal ramp (4/4 checks pass on both surfaces). Steel Teal as body text measured **3.72:1** → darkened to `#456B7A`; Warm Amber as a chart mark measured **2.44:1**, below even the 3:1 mark floor → darkened to `#A85F18`. None of these were visible by eye. See DECISIONS.
  - **Verified by looking at it**, not by assuming: rendered in headless Chrome at full page height and read back as an image, top and tail. A separate throwaway probe parsed every SVG bar path and confirmed width is proportional to value (constant 56.44 px/unit across the 10 rule bars, 46.18 across the 6 class bars) — which corrected a misread from the downsampled screenshot.
  - **No regression:** Phases 0/1/2/3 re-run green (39/0, 13/0, 22/0, 17/0). No new dependencies — `csv`, `html`, `datetime`, `pathlib` from the standard library.

- **2026-07-21 — Phase 5a complete (this session): the one-command demo.** Joel split Phase 5 at kickoff — demo now, portfolio close next session — so each half gets a full sitting.
  - **`scripts/run_demo.py`** — the whole pipeline in one command, 7 steps in **45.2s** from a cleared working tree. The chain lives in code as `STEPS` (`script, args, needs_ai, produces`), which is what makes the README checkable against it. Preflight checks Ollama up front (classify.py is 40s in before it would otherwise notice), stops on the first non-zero exit, and reports which step died.
  - **`--no-ai` is real, not decorative.** Verified properly: the first attempt passed only because a stale `classifications.csv` was still on disk. Deleting it and rerunning gave the true no-model result — **30 exceptions across 23 files, 18 clean, 1.4s, exit 0**, exactly Phase 2's standalone numbers.
  - **Fresh-clone date guard.** `run_demo.py` reads the date-anomaly paths from `seeded-errors.json` and warns if their mtimes have been reset. Git stores no mtimes, so a cloner silently gets 37 findings instead of 39 with no way to tell which side is wrong. See DECISIONS.
  - **`README.md`** — leads with the deliverable (hero screenshot), one command, the 7-step table, a **results table the gate recomputes**, what it gets right, what it does not do, requirements, repo map, and the gate table.
  - **`scripts/capture_screenshots.py`** — the capture recipe was prose in STATUS; it is now one command. Renders via headless Chrome, auto-crops the trailing background, cuts the hero, and writes `docs/report-snapshot.json` pinning 21 figures. Two self-checks that would otherwise fail silently: the render must be **taller** than the content (else the report is cut off), and the hero crop line must land in a **gutter** (else a card is sliced in half).
  - **`docs/qc-report-hero.png`** (1100×952, 55 KB) and **`docs/qc-report-full.png`** (1100×7360, 600 KB) committed. Content ends at row 7308 of a 7700px render — measured, so "the whole report" is a fact rather than a hope.
  - **`scripts/qc_phase5.py` — 85 checks.** The chain (order, completeness, an absence check that no pipeline script is missing from the demo, `--no-ai` filtering); a from-scratch run with every artifact cleared first, proven read-only against the intake; the README (every step documented **in order**, every referenced path exists, every documented flag accepted by its script, the 10-row results table recomputed against the live report, and this gate's own check count); the screenshots (dimensions vs the capture record, 21 pinned figures vs the live report, not gitignored); hygiene.
  - **Two negative tests are built into the gate permanently**, because both guard behavior that is invisible on a good run: a synthetic 3-step chain whose middle step exits 3 (the marker file proves step 3 never ran), and the date guard fired against a synthetic answer key.

## Next
**Phase 5b — portfolio close. Not started.** 5a is pushed; nothing outstanding to clean up first.

Phase 5b scope:
- Finalize the case study via the **case-study skill** → `portfolio/doc-intake-qc.md`. `CASE-STUDY-DRAFT.md` is current through 5a.
- **qc-gate skill** pass on the case study; zero typos.
- `SOP-DRAFT.md` final pass via the sop-builder skill (it is current through 5a — the demo section was added this session).
- Optional Loom walkthrough — **Joel has to record this himself**; it is the one Phase 5 item an agent cannot do.

**Do not redo these in 5b — they are done and gate-enforced:**
- The one-command demo, the README, and the committed screenshots.
- The README's numbers: they are recomputed from the live pipeline by `qc_phase5.py`, so do not hand-edit a figure — change the code and let the gate tell you the new value.
- The gate's own check count (85) is asserted against the README. Adding a check means updating that row.

**Where the report already stands on the things STATUS warned about last session** — all handled, do not redo:
- Access time: stated plainly as not preserved and not preservable, with the "capture it at acquisition" advice. **Gate-enforced** — removing it fails the build.
- Owner: not claimed; the report says ownership may fall back to the copying account on a multi-user collection.
- Naming gap: stated as reported-never-fixed, with all 9 NAMING findings still present in the organized copy.
- Cross-reference: has its own section explaining what it records and why it matters.
- Three axes: rendered as a comparison table plus the divergence file shown landing in `contract` / `Dana Cruz` / `K. Aguilar (Legal)`.

**Measured copy-method facts, so nobody re-derives them:** `robocopy /COPY:DATSO /DCOPY:DAT` preserves created, modified, accessed, ACL (SDDL identical), owner and alternate data streams, unelevated, exit 1 = success; **`/COPYALL` fails unelevated with exit 16** (needs the audit privilege) and copies nothing; PowerShell `Copy-Item` and Python `copy2` both lose creation time and ACLs, but `copy2` **does** carry alternate data streams (via Win32 `CopyFile2`).

**Measured palette facts, so nobody re-derives them** (dataviz validator, Mist `#EFF6E0` / white surfaces): Sage `#AEC3B0` on Mist is **1.69:1** — unusable as a mark, let alone text; Deep Teal/Steel Teal/Sage spans **81° of hue**, so it is not a one-hue ramp; Steel Teal `#598392` as body text is **3.72:1** (fails 4.5); Warm Amber `#D98C3F` as a chart mark is **2.44:1** (fails even the 3:1 mark floor). The shipped substitutes — `#456B7A` muted text, `#A85F18` accent mark, `#96540F` accent text, and the `#0E3B4D/#2F6E85/#7FA8B5` severity ramp — all pass. Re-run with `node scripts/validate_palette.js` in the dataviz skill before changing any color.

## Blockers
- None.

## Environment note
- Python on Joel's PC: use the **`py`** launcher (3.14.5). The bare `python`/`python3` names hit a Microsoft Store stub and fail. Run scripts via Desktop Commander: `py scripts\scan.py`. The sandbox Bash has no Python at all.
- pandas **is now installed** (3.0.3, with numpy 2.5.1) — done in the Phase 2 session.
- Installed this session (were missing under `py`): `python-docx`, `openpyxl`, `reportlab` (generator deps — restored) and `pypdf` (for reading PDF authors in Phase 2). `pillow` was already present.
- **Hardware (verified 2026-07-21, for the Phase 3 local-model decision):** NVIDIA RTX 5070, **12,227 MiB VRAM** (`nvidia-smi`; note `Win32_VideoController` misreports this as 4 GB — the known 32-bit WMI overflow, don't trust it), 31.6 GB system RAM, Ryzen 5 7500F (6 cores). Comfortably runs an 8B-class model; cannot run DeepSeek V4 (284B/1.6T).
- **Ollama is installed and running** (verified 2026-07-21). Model in use: **`gemma4:12b`** — 11.9B params, Q4_K_M, 7.04 GB. `ollama` is **not on the PATH** of a non-interactive shell; the binary lives at `%LOCALAPPDATA%\Programs\Ollama\ollama.exe`. Easiest check that it's up: `Invoke-RestMethod http://127.0.0.1:11434/api/tags`.
- `classify.py` and `qc_phase3.py` need Ollama running. `scan.py`, `rules.py`, and the Phase 0/1/2 gates do **not** — Phase 2 still stands alone on a machine with no model installed.
- **Viewing the report while building it.** The in-app browser pane **cannot screenshot local `file://` pages** (they render as static snapshots) — don't burn turns on it. This recipe is now a script: **`py scripts/capture_screenshots.py`** (headless Chrome → auto-crop → hero → pinned-figure snapshot). Run it after any change to `report.py`, or the gate will fail with `screenshot is stale`. The underlying command, if you need it by hand: `& "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless=new --disable-gpu --hide-scrollbars --screenshot="out.png" --window-size=1100,7700 "file:///…/qc-report.html"`, then read the PNG. The report renders ~7,308px tall, so crop with pillow before reading or the text downsamples into mush.
- **PowerShell gotcha, measured this session:** appending `2>$null` to the headless-Chrome call made it write **no screenshot at all** (the tool docs warn about redirecting a native exe's stderr in PS 5.1). Drop the redirect. Also, `Set-Content -Encoding utf8` writes a **BOM**, which crashes `json.loads` — use `[System.IO.File]::WriteAllText(...)` when patching a JSON file for a negative test.
- **Phase 5a added one dependency for a build-time tool only:** `capture_screenshots.py` uses **pillow** (already installed, 12.2.0) and Chrome. Nothing in the pipeline itself gained a dependency — `run_demo.py` and `qc_phase5.py` are pure standard library.
- **Phase 4c added no dependencies.** `report.py` is pure standard library (`csv`, `html`, `datetime`, `pathlib`) plus imports from `organize.py` and `rules.py`. It needs all three `organize.py --by <mode>` runs to have happened first, because it reads all three cross-references. `node` (v24.15.0) is present and is what runs the dataviz palette validator — a build-time tool only, not a project dependency.
- **Phase 4 added no dependencies.** `organize.py` is pure standard library (`argparse`, `csv`, `shutil`, `fnmatch`, `pathlib`) — no pandas, because it only ever walks rows in order. `qc_phase4.py` needs Ollama only because it reruns `classify.py` as part of the fresh chain; `organize.py --by author` and `--by custodian` run on a machine with no model at all.
- **Shell note, corrected 2026-07-21 (Phase 4b session):** the PowerShell tool runs on Joel's real PC — `py`, `robocopy`, `icacls` and **`git push`** all work directly from it. The earlier note claiming pushes need Desktop Commander for cached GitHub credentials was wrong: `git push origin main` succeeded straight from PowerShell. Desktop Commander remains useful for the `.git/HEAD.lock` cleanup issue when git is run from a *sandboxed* shell.

## QC gate results
| Phase | Result | Evidence |
|---|---|---|
| 5a (demo + README + screenshots) | PASS | `py scripts/qc_phase5.py` → **PASS: 85, FAIL: 0** (exit 0, 38.7s). From-scratch run with all five generated artifacts deleted first: `run_demo.py` exits 0, produces all 7 declared artifacts, and leaves the intake **byte-for-byte unchanged**. README verified against `run_demo.STEPS`: all 7 commands documented **in the order the runner runs them**, 12 referenced paths all exist, 17 documented commands' flags all accepted by their scripts, all 6 gate scripts listed. The **10-row results table recomputed** against the live report's `data-qc` figures. Screenshots: dimensions match the capture record, full page proven to be the whole report (7360px vs the 952px hero), **21 pinned figures still current**, and all three assets confirmed **not** gitignored. **Negative tests (3), each isolating one claim:** (A) changing one README figure 25→26 → **exactly 1** failure naming that figure; (B) editing one pinned figure in the snapshot → **exactly 1** failure, `screenshot is stale: sev_high was 10, is now 11`, with the re-capture command attached; (C) moving `report.py` before the organize steps → 21 failures, and `report.py` **refused to render** (`RECONCILIATION FAILED: crossref-by-class.csv is missing`), which disproved a README sentence claiming it would fail quietly — the README was corrected. All reverted, 85/0 restored. Prior gates re-run green after: 39/0, 13/0, 22/0, 17/0, 131/0. **Two gate bugs found and fixed during the run:** the hygiene check matched its own source (the marker words appeared in its pattern and its label — now assembled from fragments), and the gitignore check read `organized/` as *not* ignored whenever the folder was absent, which is the exact state the gate runs in (`Path` drops the trailing slash that makes a directory rule match; verified with `git check-ignore` both ways). |
| 0 | PASS | `python3 scripts/qc_phase0.py` → PASS: 39, FAIL: 0. Answer key complete (generated with the fixture, same source of truth); all 9 planned exception types present; fixture committed untouched (`7f7fd7e`, fsck clean). |
| 2 | PASS | `py scripts/qc_phase2.py` → PASS: 22, FAIL: 0 (exit 0). 41 files → 30 exceptions across 23 files, 18 clean. Every one of the 21 seeded errors caught by its owning rule (absence check vs `seeded-errors.json`); **zero false positives** — no clean file flagged; flagged + clean = 41 reconciles with disk and answer key; author column 0 mismatches across 41; all rule_ids/severities valid, no blank details, no phantom or duplicate rows; fixture byte-for-byte unchanged. **Negative test:** disabling ZERO_BYTE produced FAIL 4 (exit 1) naming both empty files — gate proven to detect misses, then reverted. |
| 3 | PASS | `py scripts/qc_phase3.py` → PASS: 17, FAIL: 0 (exit 0). Runs scan → classify → rules fresh. **Accuracy 32/32** on every classifiable file vs the generated `classes` answer key, including both traps. All 9 no-text files reported `unclassified` — no label invented; inversely, no readable file left unclassified. The exceptions absence check is **recomputed independently** from `classifications.csv` rather than trusted from `rules.py` (9 rows, exact match). Labels all on-list, confidence in 0.0–1.0, no file labeled without ≥20 chars of text. Determinism: 8 sampled documents reclassified to identical labels. Endpoint asserted loopback → API cost $0. Fixture byte-for-byte unchanged. **Negative tests (3):** planting a wrong-folder label and a 0.40 confidence made `CLASS_CONFLICT` and `LOW_CONFIDENCE` fire (both are silent on the real fixture, so both needed proving); reintroducing the naive text fallback made the gate **fail with exit 1**, naming all 5 photos and the archive as invented labels with `unclassified` dropping 9→3. All reverted and re-verified green. |
| 4c (QC report) | PASS | `py scripts/qc_phase4.py` → **PASS: 131, FAIL: 0** (exit 0). Supersedes the 73-check run below; every 4a and 4b check is still in there and still green. New in 4c (58 checks): **21 headline figures recomputed from the CSVs and compared against the values parsed back out of the rendered HTML** via `data-qc` attributes — the gate never reads report.py's own arithmetic; report.py's 8 internal reconciliation checks all green and rendered into the deliverable; **date fidelity re-measured live at 246/246** (41 × 3 modes × 2 fields); all 7 required sections present; every fired rule explained in client language; divergence file shown in 3 distinct buckets; the 4 honest-limitation statements asserted **present** (absence is the failure mode); 5 patterns proving no external asset, no `<script>`, no `@import`. **Negative tests (3), each isolating one claim:** (A) inflating the hero figure by one → **exactly 1** failure, `HTML says 26, recomputed 25`, while report.py's own reconciliation still passed 8/8 — proving the independent recomputation is what discriminates; (B) replacing the access-time caveat with the plausible-but-false "All timestamps are carried through faithfully" → **exactly 1** failure, catching a report that over-claims; (C) adding a Google Fonts `<link>` → **exactly 1** failure, `no remote stylesheet`. All reverted, 131/0 restored. A **fourth failure was found and fixed during the run**: the first draft checked `"MISMATCH" not in doc`, which fired on the rule id `EXT_MISMATCH` — a substring bug in the *check*, now matched against the rendered cell instead. Phases 0/1/2/3 re-run green after: 39/0, 13/0, 22/0, 17/0. Report verified **visually** (headless Chrome full-page render, read back top and tail) and **geometrically** (a probe parsed every SVG bar path: constant 56.44 px/unit across the 10 rule bars, 46.18 across the 6 class bars). |
| 4b (metadata fidelity) | PASS (superseded by 4c above) | `py scripts/qc_phase4.py` → **PASS: 73, FAIL: 0** (exit 0). Supersedes the 36-check run below; every 4a check is still in there and still green. New in 4b: **creation timestamps preserved on all 41 copies in all 3 modes** (123 comparisons, exact to 2ms); original intake paths reconstructable by asking the filesystem, not our own mapping; nesting proven real (copies sit 4+ folders deep, so the structure check can't pass on a flat tree); cross-reference reconciles 41 rows per mode against manifest, disk, and scan-time hashes; **copy-engine probe** on a planted file carrying an alternate data stream, a non-inherited ACE and three distinct timestamps — ACL SDDL identical, ADS carried, all three timestamps carried, bytes identical; collision preflight proven **both ways** (refuses a synthetic clash, passes the real plan). **Negative tests (3), each isolating one claim:** (A) forcing the copy2 fallback → 4 failures, all three creation checks plus the probe, while `modified` still passed — so the creation check is what discriminates; (B) flattening the layout → 15 failures incl. "nesting survived: 0 copies 4+ deep"; (C) dropping `S`/`O` from the robocopy flags → **exactly 1** failure, the ACL probe. All reverted, 73/0 restored. **Test C also proved the owner check vacuous** on a single-user machine — relabelled "owner not clobbered" and logged as an open RISK rather than counted as a win. Phases 0/1/2/3 re-run green after: 39/0, 13/0, 22/0, 17/0. |
| 4 (part 1, organize) | PASS (superseded by 4b above) | `py scripts/qc_phase4.py` → PASS: 36, FAIL: 0 (exit 0). Runs scan → classify → rules → organize ×3 fresh. All three modes: **41 copies == manifest == disk**, every copy byte-identical to its original (41 hashes per mode), every mtime preserved, every file in the bucket its axis dictates, `Unassigned` holding **exactly** the files with no value on that axis (9 / 19 / 9). Buckets recomputed from the generated answer key for class and author, so the check is independent of `organize.py`. Custodian precedence pinned by four named cases (specific rule beats folder rule; sibling still gets folder rule; root-level system export; unmatched → Unassigned). Divergence file lands in 3 different buckets across the 3 modes; no value appears as both an author and a custodian. Intake byte-for-byte unchanged, all 41 originals still in place. Idempotent on rerun. **Negative tests (3):** `copy2`→`copy` failed all three mtime checks; making the class axis guess `other` instead of `Unassigned` failed with exit 1 naming all 9 unlabelable files; the `--out` guard was proven to **discriminate** (raises on the intake root, a subfolder, a deep path, and a `..` traversal; allows two legitimate paths) after the classifier correctly blocked disabling it. All reverted and re-verified green. Tree also counted independently in PowerShell: 41 files in 6 / 6 / 8 buckets. |
| 1 | PASS | `py scripts/qc_phase1.py` → Scanned 41 files; PASS: 13, FAIL: 0. Re-run after the Phase 2 `scan.py` change — no regression. Row count 41 == disk == answer-key file_count; path-set exact match (no missed/phantom rows); all 3 exact-dupe pairs share a hash; no blank hashes/types; zip not expanded; fixture byte-for-byte unchanged before/after scan (read-only proven). |

## Handoff notes for next agent
Read PLAN.md and DECISIONS.md before touching anything. Follow PROJECT-STANDARD.md (exit ritual is mandatory).
- **Never modify `mock-intake/` or the answer key by hand** — change `scripts/make_mock_data.py` and regenerate (then note that hashes rotate per run; dupe pairs still match within a run).
- After any fresh clone, rerun the generator to restore the two date-anomaly mtimes (git doesn't preserve them).
- Git from the sandbox works but can't delete its own lock/temp files on the mounted folder — if a commit leaves `.git/HEAD.lock` behind, remove it via Desktop Commander (runs on Joel's real PC). Pushing works via Desktop Commander (Joel's cached GitHub creds).
